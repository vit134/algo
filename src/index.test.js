import {describe, it} from 'mocha';
import {expect, assert} from 'chai';

import Algo from './index';

const entry = [14, 23, 1, 4, 3, 16, 20, 43, 34, 55, 76];
const algo = new Algo(entry);

describe('Algo', () => {
    it('should init Algo', () => {
        expect(alog).not.be.equal(undefined);
    });
});
